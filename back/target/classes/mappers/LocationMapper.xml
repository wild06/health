<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unity.back.mapper.LocationMapper">

    <resultMap id="LocationMap" type="com.unity.back.entity.Location">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="deviceId" column="device_id"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="timestamp" column="timestamp" javaType="java.time.LocalDateTime"/>
    </resultMap>

    <sql id="Cols">id, user_id, device_id, latitude, longitude, timestamp</sql>

    <insert id="insert" parameterType="com.unity.back.entity.Location" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO location (user_id, device_id, latitude, longitude, timestamp)
        VALUES (#{userId}, #{deviceId}, #{latitude}, #{longitude}, #{timestamp})
    </insert>

    <select id="findLatestByDevice" parameterType="long" resultMap="LocationMap">
        SELECT <include refid="Cols"/>
        FROM location
        WHERE device_id = #{deviceId}
        ORDER BY timestamp DESC
        LIMIT 1
    </select>

    <select id="findHistory" resultMap="LocationMap">
        SELECT <include refid="Cols"/>
        FROM location
        WHERE device_id = #{deviceId}
          AND timestamp BETWEEN #{start} AND #{end}
        ORDER BY timestamp ASC
    </select>
</mapper>
